# 化学侦检数据平台业务逻辑说明书

> **版本：V1.8**  
> **文档定位：平台业务开发基线**  
> **核心原则：设备是唯一现场对象；Telemetry 负责实时状态；Events 负责报警和执行结果；Notify 负责平台下行控制。**

---

# 1. 平台业务目标

平台围绕设备完成：

```text
设备自动发现
在线 / 离线
设备分组
实时地图
实时检测状态
报警
通信指挥
训练与模拟
历史复盘
审计
```

平台需要能够回答：

```text
哪些设备在线？
哪些设备离线？
每台设备在哪里？
每台设备属于哪个小组？
当前处于正常监测还是训练模式？
设备当前实时浓度是多少？
哪台设备发生了报警？
报警物是什么？
报警当前浓度是多少？
本次报警最高浓度是多少？
报警持续多久？
报警是否已经解除？
平台向设备发送过什么？
设备最终是否执行成功？
```

---

# 2. 核心业务对象

V1 不建立独立人员对象。

设备是唯一现场业务对象。

```text
设备
 │
 ├── device_id
 ├── 设备名称 / 编号
 ├── 所属小组
 ├── 在线 / 离线
 ├── monitor / training
 ├── 实时位置
 ├── 实时浓度
 ├── PID 阈值
 ├── 当前识别物
 ├── 电量
 ├── RSSI
 ├── 跌倒状态
 ├── 报警
 └── 指令执行状态
```

地图、报警、指挥、训练、历史全部直接关联设备。

---

# 3. 设备发现与上线

平台第一次收到新的 `device_id` 的 Telemetry：

```text
收到新设备 Telemetry
      ↓
自动建立设备记录
      ↓
状态 = 在线
      ↓
默认 = 未分组
```

无需提前人工登记。

---

# 4. 在线与离线

## 4.1 在线

只要平台持续收到有效 Telemetry：

```text
刷新最后通信时间
      ↓
设备保持在线
```

## 4.2 离线

V1 默认：

```text
15 秒没有收到 Telemetry
      ↓
设备离线
```

离线后仍然保留：

```text
设备信息
所属小组
报警历史
设备轨迹
指令历史
训练历史
```

## 4.3 重新上线

```text
设备离线
   ↓
重新收到 Telemetry
   ↓
立即恢复在线
```

---

# 5. 设备分组

设备被平台发现后即可直接分组。

例如：

```text
设备01
设备02
设备03
   ↓
侦察一组
```

分组与在线状态独立：

```text
在线 / 已分组
在线 / 未分组
离线 / 已分组
```

设备离线不会自动退出小组。

小组用于：

```text
地图筛选
状态统计
小组指令
批量配置
位置共享
训练
```

---

# 6. 编队容量

业务容量要求：

```text
编队总数 ≥ 39
每编队设备上限 ≥ 10
```

因此平台第一阶段最低设计规模：

```text
39 × 10 = 390 台设备
```

平台至少应按照：

> **390 台设备同时在线**

进行设计和验收。

位置共享协议单次最多下发：

```text
16 台设备
```

可以覆盖每编队至少 10 台的业务要求，并留出扩展空间。

---

# 7. 设备模式

设备当前只有两种模式：

| mode | 含义 |
|---|---|
| `monitor` | 正常监测模式 |
| `training` | 培训 / 训练模式 |

Telemetry：

```text
message.mode
```

表示设备当前实际模式。

平台通过 Notify type=4 请求切换模式。

设备执行成功后返回 Events type=2，并在后续 Telemetry 中体现实际模式。

---

# 8. Telemetry 实时状态业务

Telemetry 回答：

> **设备现在是什么状态。**

包括：

```text
位置
实时浓度
PID 阈值
当前识别物
电量
RSSI
工作模式
跌倒状态
```

每条有效 Telemetry 都参与：

```text
设备上线 / 离线判断
地图位置刷新
设备详情刷新
实时浓度展示
电量展示
RSSI 展示
模式展示
实时状态更新
```

但：

> **Telemetry 不创建报警，也不解除报警。**

---

# 9. PID 实时数据

Telemetry 中：

```text
sensor.pid.conc
```

表示设备当前实时浓度，单位：

```text
ppm
```

```text
sensor.pid.threshold
```

表示 PID 当前阈值，单位：

```text
ppm
```

这些数据用于：

```text
设备详情
实时状态
实时浓度展示
设备当前阈值展示
```

不用于创建平台报警。

---

# 10. 报警浓度与实时浓度分开

## 10.1 设备实时浓度

来源：

```text
Telemetry.sensor.pid.conc
```

用途：

```text
实时设备状态展示
```

## 10.2 报警浓度

来源：

```text
Events type=0
message.conc
```

用途：

```text
报警当前浓度
报警最高浓度
报警历史
报警过程曲线
```

因此：

```text
Telemetry.pid.conc
≠
报警最高浓度的数据来源
```

报警最高浓度只统计：

```text
Event.conc
```

---

# 11. Events 业务

Events 回答：

> **设备明确发生了什么业务事件。**

当前事件：

| type | 业务 |
|---:|---|
| `0` | 报警开始 / 报警持续更新 |
| `1` | 报警结束 |
| `2` | 指令执行结果 |

报警类型不再由 `type` 区分。

```text
names / conc
→ 检出物质报警信息

fall_detected / magnitude
→ 跌倒报警信息
```

同一设备同一时刻只维护一个活动报警过程。

---

# 12. 统一报警业务

平台对客户统一只有：

```text
设备报警
```

不把检出物质和跌倒拆成两套独立报警生命周期。

一个设备当前报警过程中可以同时包含：

```text
报警物
当前报警浓度
最高浓度
跌倒状态
```

## 12.1 报警开始

设备发送：

```text
Events type=0
```

如果当前没有活动报警：

```text
创建新的报警
报警开始时间 = Event.timestamp
```

如果已经存在活动报警：

```text
更新当前报警
不创建第二条报警
```

## 12.2 检出物质报警信息

例如：

```json
{
  "type": 0,
  "timestamp": 1789720300,
  "message": {
    "names": ["DMMP"],
    "conc": 3.8
  }
}
```

平台显示：

```text
报警物：DMMP
当前浓度：3.8 ppm
```

物质报警期间设备可以持续发送 `type=0`。

平台实时维护：

```text
当前浓度 = 最新 Event.conc
临时最高浓度 = 已收到 Event.conc 的最大值
```

## 12.3 跌倒报警信息

例如：

```json
{
  "type": 0,
  "timestamp": 1789720320,
  "message": {
    "fall_detected": true,
    "magnitude": 1.8
  }
}
```

如果当前无活动报警，则创建报警。

如果当前已经存在物质报警，则不再新建一条报警，而是在当前报警中增加：

```text
跌倒：是
```

客户仍然只看到：

```text
设备03 报警
```

详情中再展示具体报警信息。

---

# 13. 报警结束

正常报警结束必须由设备明确发送：

```text
Events type=1
```

平台不使用“没有继续收到报警 Event”作为正常报警解除依据。

## 13.1 检出物质报警正常结束

```json
{
  "type": 1,
  "timestamp": 1789720400,
  "message": {
    "names": ["DMMP"],
    "duration": 100,
    "max_conc": 5.3
  }
}
```

平台最终记录：

```text
报警状态：已解除
解除时间：Event.timestamp
最终持续时间：100秒
最终最高浓度：5.3 ppm
```

正常结束时：

> **设备提供的 duration 和 max_conc 是最终权威结果。**

## 13.2 跌倒报警正常结束

```json
{
  "type": 1,
  "timestamp": 1789720400,
  "message": {
    "fall_detected": false,
    "duration": 90
  }
}
```

平台结束当前报警。

跌倒报警不需要最高浓度。

## 13.3 物质报警和跌倒合并结束

如果本次报警中既有报警物又发生过跌倒：

```json
{
  "type": 1,
  "timestamp": 1789720400,
  "message": {
    "names": ["DMMP"],
    "fall_detected": false,
    "duration": 100,
    "max_conc": 5.3
  }
}
```

平台用这一条结束整个当前报警过程。

---

# 14. 持续时间、最高浓度与断联

## 14.1 报警进行中的持续时间

平台为了实时展示，可以临时计算：

```text
当前时间 - 报警开始时间
```

页面可以实时显示：

```text
00:37
00:38
00:39
```

该值只是报警进行中的实时展示值。

## 14.2 正常结束后的最终持续时间

收到设备 `type=1` 后：

```text
最终持续时间 = type=1.duration
```

如果平台临时计时与设备最终 duration 有差异：

> **最终历史记录以设备 duration 为准。**

## 14.3 正常结束后的最高浓度

物质报警进行中：

```text
平台临时最高浓度 = 已收到 Event.conc 最大值
```

正常结束后：

```text
最终最高浓度 = type=1.max_conc
```

如果两者存在差异：

> **最终历史记录以设备 max_conc 为准。**

## 14.4 设备断联

设备正常情况下每 2 秒固定上报 Telemetry。

平台规则：

```text
15 秒没有 Telemetry
→ 设备离线
```

如果设备此时还存在活动报警：

```text
设备报警中
 ↓
设备断联
 ↓
平台直接结束当前报警
```

这种情况属于异常终止，不属于设备正常解除。

平台内部记录结束原因：

```text
normal
→ 收到设备 type=1

offline
→ 设备离线导致报警结束
```

## 14.5 断联时持续时间

例如：

```text
09:30:00 报警开始
09:32:10 最后一条有效 Telemetry
09:32:25 平台达到15秒并判定设备离线
```

报警结束时间记为：

```text
09:32:10
```

最终持续时间：

```text
09:32:10 - 09:30:00
= 130秒
```

离线判定等待的 15 秒不计算进报警持续时间。

## 14.6 断联时最高浓度

如果物质报警期间设备直接断联，无法发送最终 `max_conc`：

平台使用：

```text
报警期间已经收到的 Event.conc 最大值
```

作为截至断联前的最高浓度记录。

## 14.7 报警确认

确认状态仍然独立：

```text
未确认
已确认
```

报警状态：

```text
报警中
已解除
```

用户是否确认，不影响：

```text
设备正常 type=1 结束报警
或
设备离线时平台结束报警
```

---

# 15. 报警列表

平台报警中心统一显示设备报警。

建议字段：

| 设备 | 报警物 | 跌倒 | 报警时间 | 持续时间 | 当前浓度 | 最高浓度 | 报警状态 | 确认状态 |
|---|---|---|---|---|---:|---:|---|---|
| 设备03 | DMMP | 是 | 09:32:18 | 03:30 | 3.8 | 5.3 | 报警中 | 未确认 |
| 设备05 | - | 是 | 09:40:05 | 00:45 | - | - | 报警中 | 已确认 |

报警进行中：

```text
持续时间 = 平台实时计算值
最高浓度 = 平台当前已收到 Event.conc 的临时最大值
```

正常结束后：

```text
持续时间 = 设备 type=1.duration
物质报警最高浓度 = 设备 type=1.max_conc
```

设备离线结束报警时：

```text
持续时间 = 最后一条有效 Telemetry 时间 - 报警开始时间
最高浓度 = 已收到 Event.conc 最大值
```

---

# 16. 实时地图

地图以设备为唯一现场对象。

第一阶段设备状态：

```text
正常
报警
离线
```

点击设备可查看：

```text
设备ID
设备名称
所属小组
当前位置
当前模式
实时浓度
PID 阈值
当前识别物
电量
RSSI
当前报警
```

如果设备存在未解除报警：

```text
地图显示报警状态
```

如果没有报警且在线：

```text
地图显示正常
```

15 秒没有 Telemetry：

```text
地图显示离线
```

---

# 17. Notify 下行业务

平台下行分为：

| type | 业务 |
|---:|---|
| `0` | 文本通知 |
| `1` | 动作指令 |
| `2` | 参数配置 |
| `3` | 位置共享 |
| `4` | 模式切换 |

可以发送到：

```text
单设备
指定小组
全部设备
```

---

# 18. 哪些 Notify 需要执行结果

| Notify | 是否需要 command ID | 是否需要 ACK |
|---|---|---|
| 文本通知 | 是 | 是 |
| 动作指令 | 是 | 是 |
| 参数配置 | 是 | 是 |
| 位置共享 | 否 | 否 |
| 模式切换 | 是 | 是 |

原因：

> 需要平台知道设备是否真正处理或执行的消息，必须形成闭环。

位置共享属于高频状态同步，不要求每次 ACK。

---

# 19. Command ID

需要 ACK 的 Notify：

```text
平台创建时生成一个 UUID v7
```

同一次业务操作只生成一个 command ID。

## 单设备

```text
指令 A
→ 设备01
→ ACK A
```

## 小组

例如向 10 台设备发送：

```text
立即撤离
```

平台只生成：

```text
command_id=A
```

10 台设备都返回：

```text
id=A
```

平台以：

```text
command_id + device_id
```

记录每台设备结果。

## 全体

全体广播同样使用同一个 command ID。

---

# 20. 指令执行结果

设备通过：

```text
Events type=2
```

返回。

设备结果只有：

```text
success
failed
```

如果失败，可以附带：

```text
reason
```

平台业务最终显示：

```text
已发送
执行成功
执行失败
执行超时
```

其中：

```text
success → 执行成功
failed  → 执行失败
10秒未收到对应 ACK → 执行超时
```

---

# 21. 指令超时和重试

V1 默认：

```text
指令执行超时 = 10 秒
```

V1 不自动重试。

如果超时：

```text
平台显示超时
```

是否重新发送由用户决定。

---

# 22. 指令幂等

因为 MQTT QoS 1 可能出现重复投递：

```text
同一个 command ID
```

不能重复执行。

设备第一次执行后保存结果。

如果 5 分钟内再次收到相同 command ID：

```text
不重复执行
直接返回之前保存的执行结果
```

V1 建议已执行 command ID 缓存：

```text
5 分钟
```

---

# 23. 动作指令

当前动作包括：

```text
立即撤离
集合
开启静默
解除静默
前往指定坐标
```

平台支持：

```text
单设备发送
小组发送
全部设备发送
```

动作指令必须有 command ID，并等待设备 ACK。

---

# 24. 参数配置

Notify type=2 表示远程参数配置。

当前只保留：

```text
参数配置能力
```

但第一版业务不定义具体允许修改哪些参数。

后续由设备端和平台端共同确定允许远程配置的字段。

参数配置必须：

```text
有 command ID
设备返回 ACK
平台记录执行结果
审计记录
```

---

# 25. 位置共享

平台根据同组设备最新位置：

```text
设备A
设备B
设备C
 ↓
形成小组位置集合
 ↓
Notify type=3
 ↓
下发给组内设备
```

单次最多：

```text
16 台设备
```

位置共享：

```text
不需要 command ID
不需要 ACK
QoS 0
```

---

# 26. 训练与模拟业务

## 26.1 进入训练模式

```text
创建训练
      ↓
选择参与设备 / 小组
      ↓
Notify type=4
mode=training
      ↓
设备执行
      ↓
Events type=2 success
      ↓
Telemetry.mode=training
```

## 26.2 训练内容

训练可以包含：

```text
训练名称
参与设备
参与小组
模拟污染源
模拟物质
模拟浓度
影响范围
扩散条件
```

## 26.3 结束训练

```text
结束训练
      ↓
Notify type=4
mode=monitor
      ↓
设备执行
      ↓
Events type=2
      ↓
Telemetry.mode=monitor
```

## 26.4 真实报警优先

训练过程中如果设备真实产生：

```text
Events type=0
Events type=1
```

仍然按照真实报警处理。

训练不能屏蔽真实报警。

---

# 27. MQTT 业务可靠性

V1 规则：

```text
Telemetry → QoS 0
位置共享 → QoS 0

报警 Events → QoS 1
ACK Events → QoS 1
文本 Notify → QoS 1
动作 Notify → QoS 1
配置 Notify → QoS 1
模式 Notify → QoS 1
```

全部：

```text
retain=false
```

业务目的：

```text
实时状态允许下一条覆盖上一条
关键报警和指令不能轻易丢失
旧消息不能在重连后再次执行
```

---

# 28. 实时数据与历史数据

设备高频上报并不等于每一条都永久保存。

正常状态：

```text
所有 Telemetry 都实时处理
+
历史数据按周期采样
+
关键状态变化立即保存
```

报警期间：

```text
提高历史保存密度
```

报警当前浓度和最高浓度仍然以 Event.conc 为准。

---

# 29. 小组业务

平台支持：

```text
至少 39 个编队
每编队至少 10 台设备
```

小组汇总：

```text
设备总数
在线数
离线数
报警数
```

小组用于：

```text
地图筛选
小组指令
小组配置
训练
位置共享
```

---

# 30. 小组指令

用户对一个小组点击：

```text
立即撤离
```

平台生成一个 command ID。

所有目标设备共用该 ID。

最终显示：

```text
设备01  成功
设备02  成功
设备03  失败
设备04  超时
```

小组指令的最终结果是：

```text
一次用户操作
+
多台设备执行结果
```

---

# 31. 历史与任务复盘

历史围绕设备：

```text
设备轨迹
设备状态
实时浓度趋势
报警历史
指令历史
训练历史
```

检出物质报警需要能够还原：

```text
报警发生
 ↓
报警物
 ↓
Event.conc 变化
 ↓
最高浓度
 ↓
设备位置变化
 ↓
报警解除
```

指令需要能够还原：

```text
谁发送
什么时候发送
发送给谁
具体内容
每台设备最终结果
```

---

# 32. 审计

必须记录：

```text
登录
设备分组
修改设备信息
报警确认
发送文本
发送动作指令
发送参数配置
切换训练模式
开始训练
结束训练
导出历史数据
```

审计回答：

```text
谁
什么时间
对什么设备 / 小组
做了什么
结果是什么
```

---

# 33. 完整业务闭环

## 正常监测

```text
Telemetry
 ↓
自动发现设备
 ↓
设备上线
 ↓
地图实时刷新
 ↓
设备分组
 ↓
正常监测
```

## 报警

```text
Events type=0
 ↓
创建或更新当前报警
 ↓
物质报警持续发送 Event.conc
 ↓
平台实时显示当前浓度和临时最高浓度
 ↓
用户可确认
 ↓
平台可以发送处置 Notify
 ↓
Events type=1
 ↓
设备返回最终 duration
 ↓
物质报警返回最终 max_conc
 ↓
报警正常结束
```

设备断联：

```text
报警中
 ↓
15 秒无 Telemetry
 ↓
设备离线
 ↓
平台结束当前报警
 ↓
结束时间取最后一条有效 Telemetry 时间
```

## 指令

```text
平台生成 command ID
 ↓
发送 Notify
 ↓
设备执行
 ↓
Events type=2
 ↓
平台显示 success / failed
 ↓
10秒无 ACK
 ↓
timeout
```

## 训练

```text
创建训练
 ↓
Notify training
 ↓
设备 ACK
 ↓
Telemetry.mode=training
 ↓
训练过程
 ↓
Notify monitor
 ↓
设备 ACK
 ↓
Telemetry.mode=monitor
```

---

# 34. 第一阶段业务验收

## 设备在线

```text
Telemetry → 在线
15秒无 Telemetry → 离线
恢复 Telemetry → 立即在线
```

## 分组

```text
新设备 → 未分组
平台操作 → 加入小组
设备离线 → 小组关系保留
```

## 报警

```text
Events type=0
→ 创建或更新当前报警
→ 物质报警 Event.conc 更新当前浓度
→ 平台临时统计最高浓度
→ 平台实时显示持续时间
→ 用户可确认也可不确认
→ Events type=1
→ 最终持续时间采用设备 duration
→ 物质报警最高浓度采用设备 max_conc
→ 报警结束
```

## 设备报警期间断联

```text
报警中
→ 15秒无 Telemetry
→ 设备离线
→ 平台结束当前报警
→ 结束时间取最后有效 Telemetry
→ 持续时间平台计算
→ 物质报警最高浓度取已收到 Event.conc 最大值
```

## 指令

```text
Notify + command ID
→ 设备执行
→ Events type=2
→ success / failed
→ 10秒无结果 = timeout
```

## 训练

```text
Notify training
→ ACK
→ Telemetry.mode=training
→ Notify monitor
→ ACK
→ Telemetry.mode=monitor
```

## 容量

```text
≥39 编队
≥10 台/编队
≥390 台同时在线
位置共享单次最多16台
```

---

# 35. V1.8 已冻结业务规则

```text
1. 设备是唯一现场业务对象。
2. 不单独管理人员。
3. Telemetry 决定实时状态和在线状态。
4. 设备正常情况下每 2 秒上报一次 Telemetry。
5. 15 秒无 Telemetry = 离线。
6. 收到 Telemetry = 立即在线。
7. monitor = 正常监测。
8. training = 培训 / 训练。
9. PID conc / threshold 单位均为 ppm。
10. Telemetry.pid.conc 只用于设备实时状态。
11. Events type=0 = 报警开始 / 报警持续更新。
12. Events type=1 = 报警结束。
13. Events type=2 = 指令执行结果。
14. 同一设备同一时刻只维护一个活动报警。
15. 物质报警和跌倒状态可以合并在同一次报警过程中。
16. 物质报警期间 Event.conc 用于报警当前浓度实时展示。
17. 报警进行中平台可以临时计算持续时间。
18. 报警进行中平台可以临时统计已收到 Event.conc 的最高值。
19. 正常结束必须由设备发送 type=1。
20. 正常结束最终持续时间以 type=1.duration 为准。
21. 物质报警正常结束最终最高浓度以 type=1.max_conc 为准。
22. 用户报警确认与报警结束相互独立。
23. 设备离线时平台直接结束当前报警。
24. 设备离线导致报警结束时：
    - 结束时间取最后一条有效 Telemetry 时间
    - 持续时间由平台计算
    - 物质报警最高浓度取已收到 Event.conc 最大值
25. Notify type=0~4 分别是文本、动作、配置、位置、模式。
26. 文本/动作/配置/模式必须有 command ID 和 ACK。
27. 位置共享不需要 command ID 和 ACK。
28. command ID 使用 UUID v7。
29. 同一次小组/全体指令使用同一个 command ID。
30. 平台通过 command_id + device_id 区分每台设备执行结果。
31. ACK result 只有 success / failed。
32. timeout 由平台判定。
33. 指令超时默认 10 秒。
34. V1 不自动重试。
35. command ID 用于幂等。
36. 建议设备缓存已执行 ID 5 分钟。
37. Telemetry 和位置共享 QoS 0。
38. Events 和需确认的 Notify QoS 1。
39. 所有消息 retain=false。
40. 平台最低按照 390 台设备同时在线设计。
41. 单次位置共享最多 16 台。
```

---

# 36. 暂未定义业务项

当前仍保留：

1. 参数配置允许修改的完整参数；
2. `failed` 原因是否需要标准错误码；
3. 后续新增动作类型；
4. 后续新增事件类型；
5. 超过 390 台设备后的容量等级。
